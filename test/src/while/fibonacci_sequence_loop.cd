fun fibonacci_sequenece1(n) {
	println(n)
	var prev = 1
	var prevprev = 0
	var cur = 1
	var i = 1
	while (i <= n - 2) {
		cur = prev + prevprev
		prevprev = prev
		prev = cur
		i = i + 1
	}
	return cur
}

assert fibonacci_sequenece1(10) == 34