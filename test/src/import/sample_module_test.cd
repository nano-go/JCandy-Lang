import "sample_module" as sample

println(sample.name())
assert sample.name() == "sample_module"
sample.genRandArr(100, range(0, 15))
sample.genRandInteger(0, 15)

try {
	sample.a()
} intercept AttributeError {}
try {
	sample.b()
} intercept AttributeError {}