import "random" as random

for (i in range(0, 10000)) {
	var rv = random.randomInt(0, 100)
	assert rv >= 0 && rv < 100
}

for (i in range(0, 10000)) {
	var rv = random.randomFloat(0.0, 100)
	assert rv >= 0 && rv < 100
}