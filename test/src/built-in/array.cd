fun arr_test() {
	var arr = []
	arr.append(arr)
	
	assert str(arr) == "[[...]]"
	arr.deleteAt(0)
	
	arr = Array(1000, lambda i -> i)
	assert arr.reverse() == Array(1000, lambda i -> 1000-i-1)
	assert arr.reverse().copyRange(0, 15) == Array(15, lambda i -> i)
	assert arr.copy() == arr
}

arr_test()